import React from 'react';

const SwitchFormHeader = ({children}) => {
    return (
        <div className='bg-light-utopid p-5 text-dark-utopid'>
            {children}
        </div>
    );
};

export default SwitchFormHeader;